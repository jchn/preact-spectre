---
name: Label
---

# Label

import Label from './Label'
import { Playground, PropsTable } from 'docz'

## Properties

<PropsTable of={Label} />

### Default

The default Label:

<Playground>
  <Label>default label</Label>
</Playground>

### Rounded

Add the `rounded` prop to give the Label rounded corners:

<Playground>
  <Label rounded>rounded label</Label>
</Playground>

### Kind

Add the `kind` property to change the color of the label, the value should be one of `primary`, `secondary`, `success`, `warning` or `error`:

<Playground>
  <Label kind="primary">primary label</Label>
  <Label kind="secondary">secondary label</Label>
  <Label kind="success">success label</Label>
  <Label kind="warning">warning label</Label>
  <Label kind="error">error label</Label>
</Playground>
