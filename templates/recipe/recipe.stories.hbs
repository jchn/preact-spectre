import { h } from 'preact'
import centered from '@storybook/addon-centered/preact'
import { storiesOf } from '@storybook/preact'

storiesOf('Recipes|{{pascalCase name}}', module)
.addDecorator(centered)
.add('Default', () => (
<div>{{pascalCase name}}</div>
))